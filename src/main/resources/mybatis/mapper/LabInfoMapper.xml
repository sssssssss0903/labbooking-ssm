<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.labbooking.ssm.mapper.LabInfoMapper">
    <resultMap id="BaseResultMap" type="com.labbooking.ssm.domain.LabInfo">
        <id column="id" property="id"/>
        <result column="lab_name" property="labName"/>
        <result column="location" property="location"/>
        <result column="manager_id" property="managerId"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="listByManagerId" parameterType="long" resultMap="BaseResultMap">
        select id, lab_name, location, manager_id, status
        from lab_info
        where manager_id = #{managerId}
        order by lab_name asc
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select id, lab_name, location, manager_id, status from lab_info order by id desc
    </select>
    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        select id, lab_name, location, manager_id, status from lab_info where id = #{id}
    </select>
    <insert id="insert" parameterType="com.labbooking.ssm.domain.LabInfo" useGeneratedKeys="true" keyProperty="id">
        insert into lab_info (lab_name, location, manager_id, status) values (#{labName}, #{location}, #{managerId}, #{status})
    </insert>
    <update id="update" parameterType="com.labbooking.ssm.domain.LabInfo">
        update lab_info
        <set>
            <if test="labName != null">lab_name = #{labName},</if>
            <if test="location != null">location = #{location},</if>
            <if test="managerId != null">manager_id = #{managerId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where id = #{id}
    </update>
</mapper>


