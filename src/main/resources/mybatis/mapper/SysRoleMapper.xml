<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.labbooking.ssm.mapper.SysRoleMapper">
    <select id="selectRoleCodesByUserId" parameterType="long" resultType="string">
        select r.role_code
        from sys_role r
                 inner join sys_user_role ur on r.id = ur.role_id
        where ur.user_id = #{userId}
    </select>
    <select id="selectIdByCode" parameterType="string" resultType="long">
        select id from sys_role where role_code = #{roleCode} limit 1
    </select>
    <select id="selectAll" resultType="com.labbooking.ssm.domain.SysRole">
        select id, role_code, role_name, description
        from sys_role
        order by id
    </select>
</mapper>


